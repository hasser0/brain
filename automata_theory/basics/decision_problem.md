+ [Language](/automata_theory/basics/language.md)

## Decision problem

Given a language of an alphabet $L\subseteq \Sigma^*$, a word of the same
alphabet $w\in\Sigma^*$ the decision problem of $w$ with respect to $L$ is
whether $L$ accepts $w$ or not

