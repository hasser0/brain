## Sorting

+ Merge sort
+ Quick sort
    + Pivot strategies
        + Naive
        + Lomuto
        + Hoare

Every sorting algorithm can abstracted to arbitrary input using a function $f$
that returns for any pair of elements $<$, $>$ xor $=$

