## Stable algorithm

An stable sorting algorithm preserves input order on ties without extra engineer
on the keys. Any unstable algorithm could become stable by adding the input
order as secondary key.
