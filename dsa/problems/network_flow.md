## Network flow

Given a weighted network, the values at each edge can be thought of as exchange
capacity between nodes. The network flow problem ask about the maximum amount
that can be sent from a vertex called source to another called sink

