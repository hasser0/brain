## Virtual address

Virtual address refers to memory direction at the process level. Memory
virtualization decouples physical address from virtual address so that the
second could points to an specific value ignoring the first.

