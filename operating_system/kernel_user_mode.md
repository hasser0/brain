## Kernel and User Mode

Kernel mode is a hardware mode that allows any operations, including IO
operations. User mode is restricted to only CPU and memory operations only and
work along with kernel mode to secure the operating system; kernel mode is only
executed by the OS, if it attempts to be secure.

