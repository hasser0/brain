## Transaction

A transaction is the minimal unit of work in database required to work with highly
concurrent systems, simulating a serial execution. It is an abstraction that
might be implemented in multiple ways.

