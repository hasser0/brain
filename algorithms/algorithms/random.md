+ [Quicksort](/algorithms/sorting/quicksort.md)

## Random algorithms

A random algorithm is an algorithm which use randomness as part of its logic,
for example quicksort could use randomization as part of its logic to ensure it
runs on $O(n\log n)$ with high probability

