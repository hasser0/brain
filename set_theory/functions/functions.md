+ [Relations](/set_theory/relations/relations.md)

## Functions

A function is a relation $F$ such that if $x\in\text{dom }F$ then exists one and
only one $y\in\text{ran }F$ that $xFy$.

+ $F(x) = y$ is the same as $xFy$
+ $F:A\mapsto B$

Note that any function $F: A\mapsto B\subseteq A\times B$

