## Unsigned binary

Given a sequence of binary symbols $b_n\dots b_0$ that represents a unsigned
binary number(a non negative integer number), the corresponding value in decimal
is calculated as follows
$$
(b_n\dots b_0)_2 = \sum_{i=0}^n 2^i \cdot b_i
$$

