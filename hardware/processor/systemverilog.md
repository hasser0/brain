## Systemverilog keywords

+ `module` / `endmodule`
+ `begin` / `end`
+ `case` / `endcase`
+ `if` / `else if` / `else`

+ `always_comb`
+ `always_ff`
+ `posedge`
+ `always_comb`
+ `assign`

+ `initial`
+ `assert`
+ `$error`
+ `$display`
+ `#n`

