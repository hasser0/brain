## Memory management unit

Memory management unit is a part of the processor that works closely with the
operating system translating virtual address into physical address. In general
the input to a memory management unit is the virtual address, along with certain
process address space paremeter and outputs the physical address with some flags
that alert from fault segmentation or not allowed operations for the specified
address.
