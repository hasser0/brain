+ [Chip](/hardware/basics/chip.md)

## Multiplexor

The multiplexor is a chip with inputs $a_0$, $a_1$ and $s$ and outputs

+ $a_0$ when $s=0$
+ $a_1$ when $s=1$

Generalization for arrays $a[2^n]$ and $s[n]$ are similar

